import 'package:flutter/material.dart';

class MapLogsPage extends StatelessWidget {
  const MapLogsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Maps Logs'),
    );
  }
}
